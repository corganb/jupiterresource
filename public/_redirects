# Redirect www subdomain to apex (Cloudflare Pages _redirects format)
# Apex is the canonical domain set on the page <link rel="canonical">.
https://www.jupiterresource.net/*  https://jupiterresource.net/:splat  301
